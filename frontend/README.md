# Frontend

React + Redux Toolkit checkout SPA (Vite). See the [repo root README](../README.md) for the full architecture, setup, environment variables, and deployment guide.

Quick reference:

```bash
npm install
npm run dev        # http://localhost:5173
npm run test:cov
npm run build
```
